// 1. how to declare a variable using let and const
// avoid variale declaration using var
const month = 'December';
let season = 'winter';
season = spring;

// 2. conditions
// 6 basic conditions: >, <, ===, !==, <==, >==
// 2 multiple conditions: &&, ||
if(month === 'December' && season === 'spring'){
    console.log('december in winter');
} else if(month === 'December'){
    console.log('only december');
} else {

}

// 3. array declarations
const years = [2023, 2024, 2025];
years